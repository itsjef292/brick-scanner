{
  "name": "brick-scanner-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Native iOS shell for Brick Scanner. Loads the live web app over Tailscale (server.url) and adds an on-device ML Kit Data Matrix scanner for CMF box codes — far stronger than the in-browser/server decode.",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios",
    "ios": "cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor-mlkit/barcode-scanning": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}

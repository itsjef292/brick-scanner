.env
.flask_secret
.passkeys.json
.claude/
__pycache__/
*.pyc
.DS_Store

# Offline catalog: Rebrickable CSV dump + generated SQLite DB (local dev only,
# ~330MB total). Rebuild locally with: python3 build_brick_db.py
Brick Parts/
brick_parts.db
brick_parts.db.new*
templates/index.html.bak

# Raw BrickLink catalog download (manual; regenerable; Taildrop may append .xml).
# The *built* index minifig_variants.json IS committed — tiny, ships to Render.
Minifig*.txt
Minifig*.xml

# Daily catalog-refresh runtime artifacts (local dev only)
.catalog_manifest.json
.catalog_changes.json
.catalog_last_checked
catalog_refresh.log
catalog_refresh.launchd.log

# Flask app log (launchd autostart agent, local dev only)
app.log

# Owned-set purchase metadata + local minifig collection (local dev only)
.set_meta.json
.set_meta.json.tmp
.part_bins.json
.part_bins.json.tmp
.minifig_collection.json
.minifig_collection.json.tmp
.bl_minifig_variants.json
.bl_minifig_variants.json.tmp
.subtract_records.json
.subtract_records.json.tmp

# Daily minifig price-refresh logs (local dev only)
minifig_prices.log
minifig_prices.launchd.log

# Daily set price-refresh logs (local dev only)
set_prices.log
set_prices.launchd.log
.playwright-mcp/

# Monthly retirement-data refresh log (local dev only)
retirement_refresh.log
